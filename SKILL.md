---
name: init-docs
description: 为当前项目初始化或维护以仓库根目录 AGENTS.md 为入口的分层协作文档体系（spec / design-docs / exec-plans / rules / MANUAL）；适用于建立项目指引、整理已有文档、规范执行计划或补充可复用的项目规则
---

# 任务：初始化或维护 AI 协作文档体系

为当前项目建立精简、可追踪、按需加载的文档导航。仓库根目录的 `AGENTS.md` 是仓库级默认入口，但不是唯一的指令来源：全局 `AGENTS.md` 和更靠近目标文件的嵌套 `AGENTS.md` 也可能生效，越靠近目标目录的项目指引越具体。

核心原则：

1. **入口小而有用**：根 `AGENTS.md` 只保留项目定位、文档路由、工作约束、验证命令和目录作用域说明，目标控制在 30 行左右；详细规则和设计内容放在 `docs/`。
2. **按需建档和读取**：只创建探索结果支持且项目会使用的文档；先读索引，再进入与任务相关的文档，不要一次性加载整个 `docs/`。
3. **按作用域处理指令**：根 `AGENTS.md` 提供默认约定，嵌套文件只写子目录特有规则，不要复制上级内容。需求、设计和操作规则属于不同信息层，发生冲突时记录冲突并让项目负责人确认，不要用一个简单的总优先级掩盖冲突。
4. **计划可恢复**：修改源代码、配置或执行运维动作前，先完成详细执行计划；每完成一个步骤立即更新复选框和结果，使中断后的下一次任务可以从第一个未完成步骤继续。
5. **文档与代码保持一致**：代码变更影响行为、接口、架构、部署或约束时，更新对应文档；纯内部实现变化不应制造无意义的文档改动。

## 第 0 步：探索项目（先做，不要跳过）

在创建或覆盖任何项目文档前，先完成以下探索，并把结论用于文档内容：

### 0.1 确认作用域和工作区状态

- 确认当前工作目录，读取适用的全局、根目录和最近的嵌套 `AGENTS.md`；遵守更具体的本地指引，不覆盖已有指引。
- 如果是 Git 仓库，执行 `git branch --show-current` 和 `git status --short`。处于 `master` / `main` 时，先让用户切换到工作分支，或在符合项目规则且不会影响未提交工作的情况下创建分支。
- 不执行 `git commit`、`git push` 或丢弃用户未提交工作的操作，除非用户明确要求。

### 0.2 了解项目事实

阅读 README、构建和依赖文件（如 `pom.xml`、`package.json`、`go.mod` 等）、模块目录、测试与部署配置，以及已有文档的索引。至少确认：

- 产品定位：项目是什么，负责什么业务范围。
- 核心模块：各模块或子工程的职责边界。
- 技术链路：数据或请求如何在模块间流转、处理和持久化。
- 关键约束：性能、安全、兼容性、部署、数据变更和验证要求。
- 已有文档：哪些内容应纳入索引，哪些内容重复、过期或需要用户确认。

找不到 README、构建文件或某项事实时，记录信息不足，不要编造产品定位、架构或约束。

### 0.3 处理已有文档

- 保留已有文档和原有格式，优先补索引、链接和状态，不要为了符合模板而重写。
- 发现重复文档时，在执行计划中提出合并或废弃建议；未经用户确认不要删除。
- 先读取已有 `index.md` 或导航文件，再按当前任务进入具体文档。

## 第 1 步：设计最小目录骨架

不要固定生成一整套空文件。根据探索结果选择需要的部分：

```
AGENTS.md                         # 始终维护：仓库级默认入口
docs/
  ARCHITECTURE.md                 # 架构非平凡或需要跨模块说明时
  DESIGN.md                       # 存在项目级技术红线时
  MANUAL.md                       # 存在待人工执行事项时
  spec/
    index.md                      # 存在或计划维护规格文档时
  design-docs/
    index.md                      # 存在长期设计文档时
  exec-plans/
    index.md                      # 使用执行计划时，唯一的计划索引
    active/                       # 进行中的计划；有计划时创建
    completed/                    # 已完成的计划；有计划时创建
  rules/
    index.md                      # 采用项目规则时
    <按技术栈选择的规则文件>
  sql/                            # 仅数据库项目：待人工审核的 SQL 脚本
```

不要创建独立的 `docs/PLANS.md`：计划状态由 `docs/exec-plans/index.md` 和 `active/`、`completed/` 两个目录表达，避免重复索引。空目录不必人为保留；需要时再创建。

## 第 2 步：生成或更新文档

### 根目录 `AGENTS.md`

根 `AGENTS.md` 必须基于真实探索结果填写，不得留下 `<项目名>`、`TODO` 或虚构命令。它至少说明：

- 项目名称和一到两句话的产品定位。
- 只指向实际存在且与日常工作有关的文档；链接使用相对路径。
- 必须执行的构建、测试、检查或部署前验证命令。
- 执行计划规则：改源代码、配置或做运维前先完成 `docs/exec-plans/active/` 中的详细计划；每完成一步立即标记 `[x]` 并记录验证结果；中断后从第一个 `[ ]` 步骤恢复。
- 代码、文档、人工操作和目录级规则的边界。

推荐的组织顺序是：

```markdown
# 项目名

## 产品定位
一句话说明项目和业务边界。

## 文档导航
- 架构、规格、设计文档、执行计划和规则，只链接实际存在的入口。

## 工作规则
- 只按任务读取相关文档。
- 修改源代码或配置前先完成详细执行计划，并逐步更新复选框。
- 变更影响行为、接口、架构或运维方式时同步更新对应文档。

## 验证
- 列出项目真实存在的验证命令。
```

`AGENTS.md` 只描述仓库约定，不替代需求规格或完整设计文档。若某个子目录有特殊构建、测试或安全要求，在该目录添加更具体的 `AGENTS.md`，只写差异部分。

### `docs/ARCHITECTURE.md`

仅在架构需要超出 `AGENTS.md` 的说明时创建。开头用一段话说明谁生产、谁消费、如何存储；随后按模块列出职责并用反引号标识目录或模块；结尾列出全局技术约束。控制在 30 行左右，详细设计下沉到 `design-docs/`。

### `docs/DESIGN.md`

仅写基于项目技术栈和真实约束得出的可执行技术红线，标题使用 `# Design Guidelines`，每条一个 bullet。不要生成与项目无关的泛化建议。

### `docs/MANUAL.md`

只有存在需要用户确认或人工执行的事项时才创建。每条 TODO 必须是具体动作，使用 `[ ]` / `[x]`，写明前置条件和验证方式。未完成事项只阻止对应的人工或运维动作，不要阻止无关的代码阅读、计划编写或文档整理。

### `docs/spec/`

`index.md` 只索引真实存在的规格文件，表格至少包含日期、规格和说明：

```markdown
# 规格文档索引

本目录存放进入实现计划前确认过的需求规格和设计规格。

| 日期 | 规格 | 说明 |
| --- | --- | --- |
```

规格文件使用 `YYYY-MM-DD-<kebab-case-主题>-design.md` 命名，新增条目按日期倒序放在表格首行。需要区分同日多份规格时，使用 `YYYY-MM-DD-HHmm-<kebab-case-主题>-design.md`，并在文档中注明时区。

### `docs/design-docs/`

`index.md` 只链接真实存在的长期设计文档，文档多时按业务领域分组。长期设计文档使用稳定、描述性的名称；只有决策记录、事件复盘或时间快照确实需要区分时才在名称中加入 `YYYY-MM-DD` 或 `YYYY-MM-DD-HHmm`。

### `docs/exec-plans/`

`index.md` 是执行计划的唯一索引，不要再生成 `docs/PLANS.md`。内容保持简短，不写不存在的占位链接：

```markdown
# Execution Plans

## Active

## Completed
```

每个执行计划放在 `active/` 或 `completed/`，至少包含以下信息：

```markdown
# 任务名称

- Status: Active
- Created: YYYY-MM-DD HH:mm <timezone>
- Updated: YYYY-MM-DD HH:mm <timezone>
- Spec: 相关规格链接，若有

## Objective
可验证的目标。

## Scope
涉及和明确不涉及的文件、模块或环境。

## Steps
- [ ] 具体步骤、目标文件和完成条件
- [ ] 具体验证步骤

## Risks and Manual Actions
- 风险、回滚点和需要用户执行的动作。

## Resume Note
中断时记录已完成内容、当前阻塞和下一个动作。
```

执行计划生命周期：

1. 在修改源代码、配置或执行运维动作前，先把目标、范围、假设、文件、步骤、验证、风险和人工动作写完整；计划本身先完成，再开始这些动作。
2. 每完成一个步骤，立即把对应的 `[ ]` 改为 `[x]`，补充实际结果或验证命令。不要等所有工作结束后一次性更新。
3. 任务中断或换人时，先读取计划和最后一条 `Resume Note`，从第一个未完成步骤继续；范围变化时先更新计划再继续。
4. 最终验证通过后，将计划移到 `completed/`，更新 `Status`、`Updated` 和索引；不要把已完成计划长期留在 `active/`。

计划文件默认使用 `YYYY-MM-DD-<kebab-case-任务>.md`；同日同名或需要精确排序时使用 `YYYY-MM-DD-HHmm-<kebab-case-任务>.md`。时间采用项目所在时区，除执行计划和确有追踪价值的记录外，不给长期稳定文档强行加时间戳。

### `docs/rules/`

通用规则直接从本 Skill 自带的 `docs/rules/` 目录按需选择，不要在 `SKILL.md` 中重复抄写规则正文：

- 将适用的规则文件复制到项目的 `docs/rules/`，并在项目 `docs/rules/index.md` 中逐项建立链接和一句话摘要。
- `workflow-constraints.md` 可作为通用工作流基础；Java、设计模式等规则只在项目技术栈或用户明确要求时复制。
- 若项目已有本地规则，先保留并核对冲突，不要无条件覆盖；新增规则必须更新索引。
- 不要复制 `.DS_Store`、临时文件或与项目无关的技术栈规则。

### `.gitignore`

这些文档是本 Skill 生成的本地 AI 协作资料，默认不纳入 Git 管理。项目是 Git 仓库时，在生成文件的同一轮修改 `.gitignore`：

- 将本次新建的根 `AGENTS.md` 以及本次新建的 `docs/` 文件或目录加入忽略规则，不要执行 `git add`。
- 先查看已有 `.gitignore`、`git status --short` 和 `docs/` 内容。若 `docs/` 中已有项目文档，只加入本次新建文件的精确路径，不要直接忽略整个 `docs/`；只有整个目录都是本次生成内容时才使用 `/docs/`。
- 不使用 `*.md`、`docs/` 等宽泛模式误伤已有文档。已有被 Git 跟踪的文件不会因新增忽略规则自动停止跟踪，不要擅自执行 `git rm --cached`；在收尾清单中报告该冲突。
- 只把本 Skill 生成的协作文档加入忽略范围；项目源代码、用户已有文档、`.gitignore` 本身和必要的构建配置仍按项目规则处理。

## 第 3 步：收尾和验证

逐项完成以下检查：

1. 每个索引只链接真实存在的文件；没有占位链接、死链或重复的计划索引。
2. `AGENTS.md` 的产品定位、模块和命令来自探索结果；不知道的内容写入交付说明，不要猜测。
3. 执行计划的每个步骤都有 `[x]` 或明确的阻塞说明；完成计划已移入 `completed/`。
4. 项目规则只包含适用技术栈，且新规则已加入 `docs/rules/index.md`。
5. 本次新建的 AI 协作文档已加入 `.gitignore`，没有使用宽泛模式误伤已有文档，也没有执行 `git add` 或取消已有文件的跟踪。
6. 输出清单：创建或更新了哪些文件、索引了哪些已有文档、哪些事实或人工动作仍需用户补充。
7. 不执行 `git commit` 或 `git push`，改动停留在工作区由用户确认。
