---
name: index-docs
description: 为当前项目初始化或维护以根目录 AGENTS.md 为入口的本地 AI 协作文档索引，覆盖 Agent 规格、可恢复任务记录、规则和人工操作。
---

# 任务：索引项目协作文档

为当前项目建立精简、可追踪、按需加载的本地协作文档。根 `AGENTS.md` 是仓库级默认入口，但不是唯一的指令来源：全局 `AGENTS.md` 和更靠近目标文件的嵌套 `AGENTS.md` 也可能生效，越靠近目标目录的项目指引越具体。

## 信息模型

只维护下列四类核心信息，避免以多个目录描述同一件事：

1. **Agent 规格**：Agent 应如何工作，包括目标、输入输出、工具权限、状态流转、失败处理和验收标准。
2. **任务记录**：仅记录 Agent 的执行计划、实际执行记录和 handoff。
3. **规则**：必须、禁止或应当遵守的编码、Git、安全和运维约束。
4. **人工操作**：需要用户在终端、控制台或生产环境执行的精确步骤。

`architecture.md` 仅记录系统当前事实；`decisions/` 仅记录需要长期保留的关键决策。它们均为按需文档，不能作为规则或任务记录的重复副本。

## 核心原则

1. **入口小而有用**：根 `AGENTS.md` 仅保留项目定位、文档路由、关键约束、验证命令和目录作用域说明，目标控制在 30 行左右。
2. **按需建档和读取**：只创建探索结果支持且项目会使用的文档；先读入口或索引，再读取与当前任务相关的文件。
3. **规则和事实分离**：`docs/rules/` 写约束，`docs/architecture.md` 写现状，`docs/agent-spec/` 写 Agent 设计，`docs/tasks/` 写任务过程。发生冲突时记录并请项目负责人确认。
4. **任务可恢复**：任务文档只维护 Agent 可执行的动作和结果；每完成一步立即记录。任务中断后，从 `Current State` 和 `Next Action` 恢复，而非猜测上下文。用户必须执行的动作只写入 `docs/manual.md`。
5. **本地个性化**：本 Skill 生成的 `AGENTS.md` 与协作文档默认仅供当前使用者本地使用，必须加入 `.gitignore`，不得擅自纳入 Git，避免污染其他成员的 Agent 定制。
6. **文档与代码保持一致**：代码变更影响行为、接口、架构、部署或约束时，更新对应文档；纯内部实现变化不应制造无意义的文档改动。

## 第 0 步：探索项目（先做，不要跳过）

在创建或覆盖任何项目文档前，完成以下探索，并以真实结果填写文档：

1. 确认当前工作目录，读取适用的全局、根目录和最近的嵌套 `AGENTS.md`；遵守更具体的本地指引，不覆盖已有指引。
2. 若是 Git 仓库，执行 `git branch --show-current` 和 `git status --short`。处于 `master` / `main` 时，先让用户切换工作分支，或在不影响未提交工作的前提下按项目规则创建分支。
3. 阅读 README、依赖/构建文件、模块目录、测试和部署配置，以及已有文档索引。确认产品定位、模块边界、技术链路、关键约束和已有文档。
4. 找不到事实时记录信息不足，不要编造。保留已有文档与格式；发现重复内容时提出合并建议，未经用户确认不得删除。

## 第 1 步：设计最小目录骨架

不要固定生成空文件或空目录。仅创建项目实际需要的部分：

```text
AGENTS.md                         # 始终维护：本地仓库级入口
docs/
  agent-spec/                     # 有 Agent 设计规格时
    index.md
  tasks/                          # 有需要计划、记录或 handoff 的任务时
    index.md
    active/
    completed/
  rules/                          # 有项目规则时
    index.md
    <按技术栈选择的规则文件>
  manual.md                       # 有人工待办时
  architecture.md                 # 架构需要跨模块说明时
  decisions/                      # 需要长期保留关键技术决策时
    index.md
  sql/                            # 仅数据库项目：待人工审核的 SQL 脚本
```

不要创建 `DESIGN.md`、`design-docs/`、`spec/`、`exec-plans/` 或 `PLANS.md`。Agent 设计规格统一放入 `agent-spec/`；一项工作的 Agent 计划、记录和 handoff 统一放入 `tasks/`；人工操作只写入 `manual.md`。

## 第 2 步：生成或更新文档

### 根目录 `AGENTS.md`

基于真实探索结果填写，不得留下占位符、TODO 或虚构命令。只说明：

- 项目名称和一到两句话的产品定位。
- 实际存在且与日常工作有关的文档入口，链接使用相对路径。
- 必须执行的构建、测试、检查或部署前验证命令。
- 何时需要创建或更新 `docs/tasks/active/` 的任务记录。
- 代码、文档、人工操作和目录级规则的边界。

`AGENTS.md` 不替代 Agent 规格、完整架构说明或任务记录。子目录有特殊构建、测试或安全要求时，只在该目录新增差异化的 `AGENTS.md`。

### `docs/agent-spec/`

仅在项目定义或维护 Agent 时创建。`index.md` 只索引真实存在的规格文件，新增条目按更新时间倒序。规格使用稳定、描述性的 `<agent-name>.md` 命名，不按任务日期命名。

每份规格至少包含：目标与非目标、输入输出契约、工具与权限、状态流转、失败和降级处理、验收标准、关联规则。它描述 Agent 的长期设计，不能记录某次任务执行过程。

### `docs/tasks/`

`index.md` 是唯一任务索引，只列出真实存在的 Active 和 Completed 文档。一个任务文件同时是 Agent 的执行计划、执行记录和 handoff；不要再维护平行的计划或交接文档。

以下情形必须先在 `active/` 创建或更新任务文档：跨模块变更、接口或数据变更、安全相关改动，或预计需要多个可验证的 Agent 步骤的改动。微小、可独立验证的修复可省略完整计划，但仍应在最终交付中说明实际改动和验证。

任务文档不得包含用户需要执行的命令、控制台操作、人工待办或人工操作结果。发现这类事项时，只在 `docs/manual.md` 创建或更新待办；不得为了等待用户操作而让 Agent 已完成的任务继续停留在 `active/`。

任务文件使用 `YYYY-MM-DD-<kebab-case-任务>.md`；同日同名或需要排序时使用 `YYYY-MM-DD-HHmm-<kebab-case-任务>.md`。至少包含：

```markdown
---
status: active | blocked | completed
created: YYYY-MM-DD HH:mm <timezone>
updated: YYYY-MM-DD HH:mm <timezone>
branch: <branch-or-not-applicable>
spec: <relative-link-or-none>
---

# 任务名称

## Objective
可验证的目标。

## Scope
涉及和明确不涉及的文件、模块或环境。

## Plan
- [ ] 具体步骤、目标文件和完成条件
- [ ] 具体验证步骤

## Execution Record
- YYYY-MM-DD HH:mm：实际操作、结果和验证命令。

## Current State
- 已完成：
- 未完成：
- 阻塞：

## Next Action
中断后可直接执行的下一步，写明目标文件或命令。

## Verification
- 命令：
- 结果：

```

生命周期：开始前完成计划；每完成一步立即更新 checkbox 和 `Execution Record`；中断或换人时先更新 `Current State` 与 `Next Action`；Agent 动作验证通过后移动至 `completed/`，更新状态、时间和索引。范围变化时先更新任务文档再继续。需要人工操作时，只在 `manual.md` 建立待办，不在任务文档中复制或链接其内容。

### `docs/rules/`

只写可执行约束，例如代码风格、测试门槛、Git 工作流、安全和数据库运维。每条规则使用明确的“必须/禁止/应当”表达，不在其中解释架构、记录决策或描述任务过程。`index.md` 必须链接每一份实际规则并附一行摘要。

从本 Skill 的 `references/rules/` 按需复制：`workflow-constraints.md` 可作为基础；Java 或设计模式规则仅在项目技术栈或用户明确要求时复制。已有本地规则优先保留并核对冲突，禁止无条件覆盖。

### `docs/manual.md`

只有存在需要用户确认或人工执行事项时创建。未完成项使用 `[ ]`，新增项头插到 `## Pending` 首条；完成时仅改为 `[x]`，不因状态变化重新排序。

每个待办必须包含：关联的已完成或进行中任务、前置条件、精确命令及全部参数或控制台操作、预期结果和验证方式、回滚方式。人工待办只阻止对应的人工作业，不阻止无关的 Agent 任务、阅读、计划或文档整理。

### `docs/architecture.md` 与 `docs/decisions/`

`architecture.md` 仅在需要跨模块解释时创建，描述当前模块边界、数据/请求流、存储和部署事实；不写技术红线。`decisions/` 仅记录具有长期影响的方案选择及其背景、备选项、结论和后果。两者均不强制创建，也不使用任务日志替代。

### `.gitignore`

本 Skill 生成的协作文档默认是本地 Agent 上下文，必须在同一轮修改 `.gitignore`，且不得执行 `git add`：

- 忽略本次新建的 `AGENTS.md` 与协作文档的精确路径；只有整个 `docs/` 都由本次生成时才可忽略 `/docs/`。
- 修改前查看已有 `.gitignore`、`git status --short` 和 `docs/` 内容；不得使用 `*.md` 或宽泛 `docs/` 规则误伤已有项目文档。
- 已被 Git 跟踪的文件不会因此停止跟踪；不得执行 `git rm --cached`。在交付说明中报告此类冲突。

## 第 3 步：收尾和验证

1. 每个索引只链接真实存在的文件，无占位链接或平行任务索引。
2. `AGENTS.md` 的项目事实和验证命令均来自探索结果；未知信息明确报告，不猜测。
3. Active 任务均有明确 `Current State` 和 `Next Action`，且只记录 Agent 动作；Completed 任务已移入 `completed/`，不因待人工操作而停留在 Active。
4. Agent 规格、规则、架构事实、决策和任务记录没有重复承担彼此职责。
5. `docs/manual.md` 的未完成待办按最新创建时间倒序，并包含可直接执行的命令参数和验证方式。
6. 本次新建的本地协作文档已被精确忽略，没有误伤项目既有文档，也没有执行 `git add` 或取消跟踪。
7. 输出清单：创建或更新的文件、索引的已有文档，以及仍需用户补充或手动执行的事项。
8. 默认不执行 `git commit` 或 `git push`；只有用户明确要求时才执行，并报告目标分支与远程结果。
